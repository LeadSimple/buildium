=begin
#Open API, powered by Buildium

#GetAllGLAccounts) response message now includes the property `IsBankAccount`. This is a boolean property that indicates whether the general ledger account is also a bank account.  * A `Country` property has been added to all Address messages. This property contains an enumeration indicating the country of the address.     

The version of the OpenAPI document: v1

Generated by: https://openapi-generator.tech
OpenAPI Generator version: 5.3.1

=end

require 'date'
require 'time'

module Buildium
  # This object represents a filter for a lease search.
  class LeaseSearchMessage
    # Filters results to any lease whose unit belongs to the specified set of property ids.
    attr_accessor :property_ids

    # Filters results to any lease whose unit belongs to a property with a rental owner in the specified set of rental owner ids.
    attr_accessor :rental_owner_ids

    # Filters results to any lease whose unit number *contains* the specified value.
    attr_accessor :unit_number

    # Filters results to any lease whose current tenants' names *contain* the specified value.
    attr_accessor :tenant_name

    # Filters results to any lease whose start date is greater than or equal to the specified value.
    attr_accessor :lease_date_from

    # Filters results to any lease whose end date is less than or equal to the specified value.
    attr_accessor :lease_date_to

    # Filters results to any lease whose lease type matches the specified status. If no type is specified, leases with any type will be returned.
    attr_accessor :lease_types

    # Filters results to any lease whose lease term matches the specified status. If no status is specified, leases with any lease term status will be returned.
    attr_accessor :lease_statuses

    # Filters results to any lease whose created date and time are greater than or equal to the specified value. The value must be formatted as YYYY-MM-DD HH:MM:SS.
    attr_accessor :created_date_time_from

    # Filters results to any lease whose created date and time are less than or equal to the specified value. The value must be formatted as YYYY-MM-DD HH:MM:SS.
    attr_accessor :created_date_time_to

    class EnumAttributeValidator
      attr_reader :datatype
      attr_reader :allowable_values

      def initialize(datatype, allowable_values)
        @allowable_values = allowable_values.map do |value|
          case datatype.to_s
          when /Integer/i
            value.to_i
          when /Float/i
            value.to_f
          else
            value
          end
        end
      end

      def valid?(value)
        !value || allowable_values.include?(value)
      end
    end

    # Attribute mapping from ruby-style variable name to JSON key.
    def self.attribute_map
      {
        :'property_ids' => :'PropertyIds',
        :'rental_owner_ids' => :'RentalOwnerIds',
        :'unit_number' => :'UnitNumber',
        :'tenant_name' => :'TenantName',
        :'lease_date_from' => :'LeaseDateFrom',
        :'lease_date_to' => :'LeaseDateTo',
        :'lease_types' => :'LeaseTypes',
        :'lease_statuses' => :'LeaseStatuses',
        :'created_date_time_from' => :'CreatedDateTimeFrom',
        :'created_date_time_to' => :'CreatedDateTimeTo'
      }
    end

    # Returns all the JSON keys this model knows about
    def self.acceptable_attributes
      attribute_map.values
    end

    # Attribute type mapping.
    def self.openapi_types
      {
        :'property_ids' => :'Array<Integer>',
        :'rental_owner_ids' => :'Array<Integer>',
        :'unit_number' => :'String',
        :'tenant_name' => :'String',
        :'lease_date_from' => :'Date',
        :'lease_date_to' => :'Date',
        :'lease_types' => :'Array<String>',
        :'lease_statuses' => :'Array<String>',
        :'created_date_time_from' => :'Time',
        :'created_date_time_to' => :'Time'
      }
    end

    # List of attributes with nullable: true
    def self.openapi_nullable
      Set.new([
      ])
    end

    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(attributes = {})
      if (!attributes.is_a?(Hash))
        fail ArgumentError, "The input argument (attributes) must be a hash in `Buildium::LeaseSearchMessage` initialize method"
      end

      # check to see if the attribute exists and convert string to symbol for hash key
      attributes = attributes.each_with_object({}) { |(k, v), h|
        if (!self.class.attribute_map.key?(k.to_sym))
          fail ArgumentError, "`#{k}` is not a valid attribute in `Buildium::LeaseSearchMessage`. Please check the name to make sure it's valid. List of attributes: " + self.class.attribute_map.keys.inspect
        end
        h[k.to_sym] = v
      }

      if attributes.key?(:'property_ids')
        if (value = attributes[:'property_ids']).is_a?(Array)
          self.property_ids = value
        end
      end

      if attributes.key?(:'rental_owner_ids')
        if (value = attributes[:'rental_owner_ids']).is_a?(Array)
          self.rental_owner_ids = value
        end
      end

      if attributes.key?(:'unit_number')
        self.unit_number = attributes[:'unit_number']
      end

      if attributes.key?(:'tenant_name')
        self.tenant_name = attributes[:'tenant_name']
      end

      if attributes.key?(:'lease_date_from')
        self.lease_date_from = attributes[:'lease_date_from']
      end

      if attributes.key?(:'lease_date_to')
        self.lease_date_to = attributes[:'lease_date_to']
      end

      if attributes.key?(:'lease_types')
        if (value = attributes[:'lease_types']).is_a?(Array)
          self.lease_types = value
        end
      end

      if attributes.key?(:'lease_statuses')
        if (value = attributes[:'lease_statuses']).is_a?(Array)
          self.lease_statuses = value
        end
      end

      if attributes.key?(:'created_date_time_from')
        self.created_date_time_from = attributes[:'created_date_time_from']
      end

      if attributes.key?(:'created_date_time_to')
        self.created_date_time_to = attributes[:'created_date_time_to']
      end
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      invalid_properties = Array.new
      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      true
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(o)
      return true if self.equal?(o)
      self.class == o.class &&
          property_ids == o.property_ids &&
          rental_owner_ids == o.rental_owner_ids &&
          unit_number == o.unit_number &&
          tenant_name == o.tenant_name &&
          lease_date_from == o.lease_date_from &&
          lease_date_to == o.lease_date_to &&
          lease_types == o.lease_types &&
          lease_statuses == o.lease_statuses &&
          created_date_time_from == o.created_date_time_from &&
          created_date_time_to == o.created_date_time_to
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(o)
      self == o
    end

    # Calculates hash code according to all attributes.
    # @return [Integer] Hash code
    def hash
      [property_ids, rental_owner_ids, unit_number, tenant_name, lease_date_from, lease_date_to, lease_types, lease_statuses, created_date_time_from, created_date_time_to].hash
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def self.build_from_hash(attributes)
      new.build_from_hash(attributes)
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def build_from_hash(attributes)
      return nil unless attributes.is_a?(Hash)
      self.class.openapi_types.each_pair do |key, type|
        if attributes[self.class.attribute_map[key]].nil? && self.class.openapi_nullable.include?(key)
          self.send("#{key}=", nil)
        elsif type =~ /\AArray<(.*)>/i
          # check to ensure the input is an array given that the attribute
          # is documented as an array but the input is not
          if attributes[self.class.attribute_map[key]].is_a?(Array)
            self.send("#{key}=", attributes[self.class.attribute_map[key]].map { |v| _deserialize($1, v) })
          end
        elsif !attributes[self.class.attribute_map[key]].nil?
          self.send("#{key}=", _deserialize(type, attributes[self.class.attribute_map[key]]))
        end
      end

      self
    end

    # Deserializes the data based on type
    # @param string type Data type
    # @param string value Value to be deserialized
    # @return [Object] Deserialized data
    def _deserialize(type, value)
      case type.to_sym
      when :Time
        Time.parse(value)
      when :Date
        Date.parse(value)
      when :String
        value.to_s
      when :Integer
        value.to_i
      when :Float
        value.to_f
      when :Boolean
        if value.to_s =~ /\A(true|t|yes|y|1)\z/i
          true
        else
          false
        end
      when :Object
        # generic object (usually a Hash), return directly
        value
      when /\AArray<(?<inner_type>.+)>\z/
        inner_type = Regexp.last_match[:inner_type]
        value.map { |v| _deserialize(inner_type, v) }
      when /\AHash<(?<k_type>.+?), (?<v_type>.+)>\z/
        k_type = Regexp.last_match[:k_type]
        v_type = Regexp.last_match[:v_type]
        {}.tap do |hash|
          value.each do |k, v|
            hash[_deserialize(k_type, k)] = _deserialize(v_type, v)
          end
        end
      else # model
        # models (e.g. Pet) or oneOf
        klass = Buildium.const_get(type)
        klass.respond_to?(:openapi_one_of) ? klass.build(value) : klass.build_from_hash(value)
      end
    end

    # Returns the string representation of the object
    # @return [String] String presentation of the object
    def to_s
      to_hash.to_s
    end

    # to_body is an alias to to_hash (backward compatibility)
    # @return [Hash] Returns the object in the form of hash
    def to_body
      to_hash
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_hash
      hash = {}
      self.class.attribute_map.each_pair do |attr, param|
        value = self.send(attr)
        if value.nil?
          is_nullable = self.class.openapi_nullable.include?(attr)
          next if !is_nullable || (is_nullable && !instance_variable_defined?(:"@#{attr}"))
        end

        hash[param] = _to_hash(value)
      end
      hash
    end

    # Outputs non-array value in the form of hash
    # For object, use to_hash. Otherwise, just return the value
    # @param [Object] value Any valid value
    # @return [Hash] Returns the value in the form of hash
    def _to_hash(value)
      if value.is_a?(Array)
        value.compact.map { |v| _to_hash(v) }
      elsif value.is_a?(Hash)
        {}.tap do |hash|
          value.each { |k, v| hash[k] = _to_hash(v) }
        end
      elsif value.respond_to? :to_hash
        value.to_hash
      else
        value
      end
    end

  end

end
